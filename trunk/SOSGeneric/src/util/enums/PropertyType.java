package util.enums;
/**
 * The possible types of Property.
 * 
 * @author Gerben G. Meyer
 */
public enum PropertyType {
	UNKNOWN, BOOLEAN, NUMBER, TEXT, TIME, TIMEWINDOW, LOCATION, STATUS, DEPENDENCIES, HISTORY;
}
